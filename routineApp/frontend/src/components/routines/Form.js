import React, { Component } from "react";

export class Form extends Component {
  render() {
    return (
      <div>
        <h1>Add routine form</h1>
      </div>
    );
  }
}

export default Form;
